package com.github.avilysalAndCeltic.VampTra.logic;

import org.lwjgl.input.Keyboard;

public class Intro {
	private static boolean finished = false;
	
	public static void InitIntro(){
/*		while(!finished){
			//pressing those will result in finishied = true;
			if (Keyboard.isKeyDown(Keyboard.KEY_ESCAPE) || Keyboard.isKeyDown(Keyboard.KEY_SPACE) || Keyboard.isKeyDown(Keyboard.KEY_RETURN)) finished = true;
			
			//intro animation
			/*	
			 *  if(tansperancy < 0) finished = true;
			 */
//		}
		System.out.println("Intro done, moving into main menu");
	}
}
